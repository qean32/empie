type Props = {

}
export const CustomButton = ({ }: Props) => {
    return (
        <button></button>
    );
}

type Props_ = {

}
export const CustomButtonDisabled = ({ }: Props_) => {
    return (
        <button></button>
    );
}